load("@rules_zig//zig:defs.bzl", "zig_binary")

zig_binary(
    name = "normalize_nvptx_ir",
    main = "normalize_nvptx_ir.zig",
    visibility = ["//visibility:private"],
    zigopts = ["-fllvm"],
)

exports_files(["zig_ptx.bzl"])
